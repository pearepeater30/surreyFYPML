To run these applications, you will need to have the following installed:

OpenCV and its dependencies
Tensorflow and its dependencies
Pickle
sklearn and its dependencies

To run the real time detector, a webcam is needed.